doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="//unpkg.com/semantic-ui@2.4.2/dist/semantic.min.css")
        link(rel="stylesheet", href="./public/css/style.css")
        script(src="//unpkg.com/jquery@3.1.1/dist/jquery.min.js")
        script(src="//unpkg.com/semantic-ui@2.4.2/dist/semantic.min.js")
        block scripts

        title Designer App
    body
        .ui.menu(style="font-family:Courier;font-size:1.2em")
            img(src="img/logo.png", alt="Logo website D.app", style="width:7%")
            a.item(href="/", class=(currentRoute === "/" ? "active" : ""))
                i.home.icon
                | Home
            a.item(href="/realisations", class=(currentRoute === "/realisations" ? "active" : ""))
                | Réalisations
            a.item(href="/materiaux", class=(currentRoute === "/materiaux" ? "active" : ""))
                | Matériaux

            .right.menu
            a.item(href="/login", class=(currentRoute === "/login" ? "active" : ""))
                i.user.icon
                | Login
            a.item(href="/logout", class=(currentRoute === "/logout" ? "active" : ""))
                | Logout

        block content